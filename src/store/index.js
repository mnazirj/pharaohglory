import { createStore } from "vuex";
import Login from "@/store/dashboard/login";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    login: Login,
  },
});
